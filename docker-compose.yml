services:
  web:
    build: .
    ports:
      - "8002:8000"
    environment:
      - DATABASE_URL=sqlite:///./data/notitas.db
      - TRMNL_API_TOKEN=${TRMNL_API_TOKEN}
    volumes:
      - .:/app
      - ./data:/app/data
    restart: always

